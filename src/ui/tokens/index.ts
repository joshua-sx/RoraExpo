/**
 * Design Tokens - Rora Mobile Design System
 *
 * Single import for all design tokens:
 * import { colors, space, radius, type, shadow, animation, zIndex, sizing } from '@/src/ui/tokens';
 */

export {
  colors,
  space,
  radius,
  type,
  shadow,
  animation,
  zIndex,
  sizing,
  theme,
  type Theme,
} from "./theme";
